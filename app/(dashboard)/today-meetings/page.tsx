import { TodayMeetingsClient } from "@/components/today-meetings-client";

export default function TodayMeetingsPage() {
  return <TodayMeetingsClient />;
}

export const metadata = {
  title: "Today's Meetings - Kulan Space",
  description: "Today's scheduled meetings",
};